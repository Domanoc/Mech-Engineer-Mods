---A table with key value pairs
---The key is a string with 2 numbers joined with an underscore, The first number is the modload order id, the second the the file load order id
---the value has the filepath to the file
---@type table<string, string>
mod_info = mod_info
